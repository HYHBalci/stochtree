% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mr_policy_evaluation.R
\name{evaluate_policy_mr}
\alias{evaluate_policy_mr}
\title{Marginal Ratio (MR) Policy Evaluation (Fully Bayesian)}
\usage{
evaluate_policy_mr(
  policy_assignments,
  Y,
  A,
  X = NULL,
  propensity_scores = NULL,
  n_samples = 1000,
  df_weights = 5,
  prior_var_theta = 100
)
}
\arguments{
\item{policy_assignments}{A numeric vector of binary treatment assignments recommended by the policy.}

\item{Y}{Numeric vector of observed outcomes.}

\item{A}{Numeric vector of observed binary treatments.}

\item{X}{Dataframe or matrix of covariates (used for propensity modeling if missing).}

\item{propensity_scores}{Optional vector of fixed propensity scores. If NULL, an in-house Bayesian logistic regression is used.}

\item{n_samples}{Number of posterior samples to draw (default 1000).}

\item{df_weights}{Degrees of freedom for the B-spline weighting function (default 5).}

\item{prior_var_theta}{Prior variance for the B-spline coefficients (default 100).}
}
\value{
A list containing posterior samples of the OPE estimate and posterior mean weights.
}
\description{
Evaluates the expected population outcome of a deterministic treatment policy
using the Marginal Ratio (MR) Off-Policy Evaluation estimator. Uncertainty
is propagated via posterior sampling of the propensity scores (if not provided)
and the density ratio weights.
}

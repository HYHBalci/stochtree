% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standardize_X.R
\name{standardize_X_by_index}
\alias{standardize_X_by_index}
\title{Standardize and Transform Design Matrix (Enhanced)}
\usage{
standardize_X_by_index(
  X_initial,
  process_data = TRUE,
  interaction_rule = c("continuous", "continuous_or_binary", "all"),
  cat_coding_method = c("sum", "difference")
)
}
\arguments{
\item{X_initial}{A data.frame or matrix containing the predictor variables.}

\item{process_data}{A boolean flag. If \code{TRUE} (default), the function standardizes numeric
variables and creates dummy variables for categorical ones. If \code{FALSE}, it bypasses
transformations and returns the original data as a matrix, but still generates the metadata.}

\item{interaction_rule}{A character string specifying which interactions are allowed.
Must be one of:
\itemize{
\item \code{"continuous"} (Default): Allows interactions where at least one variable is continuous.
\item \code{"continuous_or_binary"}: Allows interactions where at least one variable is continuous OR binary.
\item \code{"all"}: Allows all possible two-way interactions between valid columns.
}}

\item{cat_coding_method}{A character string for categorical variable contrast coding.
Must be one of \code{"sum"} (for sum-to-zero/deviation coding) or \code{"difference"} (for successive differences).
Default is \code{"sum"}.}
}
\value{
A list containing \code{X_final}, \code{p_int}, \code{non_continous_idx_cpp}, and \code{X_final_var_info}.
}
\description{
Processes an input data.frame or matrix \code{X_initial} by
standardizing numeric columns and transforming categorical/binary columns
into a design matrix suitable for modeling. It also generates metadata about
the transformed columns and calculates the number of interaction terms based on a specified rule.
}

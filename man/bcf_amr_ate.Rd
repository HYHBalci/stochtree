% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bcf_amr_ate.R
\name{bcf_amr_ate}
\alias{bcf_amr_ate}
\title{Augmented Marginal Ratio (AMR) ATE Estimation (Fully Bayesian)}
\usage{
bcf_amr_ate(
  bcf_fit,
  Y,
  A,
  X = NULL,
  propensity_scores = NULL,
  df_weights = 5,
  prior_var_theta = 100
)
}
\arguments{
\item{bcf_fit}{A fitted BCF model object containing posterior samples of \code{tau} and \code{mu}.}

\item{Y}{Numeric vector of observed outcomes.}

\item{A}{Numeric vector of binary treatments.}

\item{X}{Dataframe or matrix of covariates (used for propensity modeling if missing).}

\item{propensity_scores}{Optional vector of fixed propensity scores. If NULL, an in-house Bayesian logistic regression is used.}

\item{df_weights}{Degrees of freedom for the B-spline weighting function (default 5).}

\item{prior_var_theta}{Prior variance for the B-spline coefficients (default 100).}
}
\value{
A list containing posterior samples of the ATE, plugin ATE, augmentation term, and posterior mean weights.
}
\description{
This function computes a doubly robust ATE estimate using Augmented Marginal
Ratio (AMR) weighting. It propagates uncertainty across the entire posterior
by estimating the ATE for every MCMC sample of the BCF outcome model,
sampling propensity scores (if not provided) using a Laplace approximation,
and sampling B-spline density ratio weights from a conjugate normal posterior.
}
